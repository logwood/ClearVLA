"""Standalone action-solver laboratories for ClearVLA.

The package is intentionally empty of mainline imports.  Individual solver
families expose their own versioned contracts under a subpackage so an
integration owner can select one explicitly without changing the current
runtime by accident.
"""

__all__: list[str] = []
