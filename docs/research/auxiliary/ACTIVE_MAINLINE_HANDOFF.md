# ClearVLA structural rebuild handoff

Updated: 2026-09-22. This records source work, not user-server job state.

## Verified starting point and recovery correction

- Isolated branch: `codex/structural-rebuild-20260921`; ordinary fast-forward only.
- Original architectural base: `0f07160d692ec8c8302880420a3d93d74512c39b`.
- Read-only experiment control: `a0e1f5d5b9736d37a51faec36039144f181d4b12`.
- This unit starts from published M4d
  `e09a6e9917c3b2fb1f9b4dce0083ea3264ad6b06`, complete remote Git tree
  `f2b4f0aa4712631c54105f0a953128199aa9ba2b`.
- The M4d Actions artifact was independently unpacked and its source recreated
  as the identical Git tree. Its supported runtime recorded 696 cases:
  695 passed, one skipped, zero errors/failures.
- The previous M5 recovery archive actually contains ZERO source files.
  Claimed unarchived M5 implementations/32 tests/782-file snapshots are not
  usable evidence. M5a in this unit is newly implemented on verified M4d.
- Local recovery Git ancestry is synthetic. It is not pushed or described as
  real remote history; publication uses the actual remote parent and exact tree.

## Current M5a source

Candidate: `configs/mainline/structural_rebuild_m5a_calvin.json`.
Read the M5a architecture section and repair-plan producer/consumer table.

The real organizer computes one soft FP32 K-plus-null binding per observation.
S/coarse/P1 use distinct object-internal multi-source reads under that law;
P2 retains its object marginal while selecting a view within each object.
Current target facts reach P1 after clean residual subtraction, independent
of predicted W motion. Recent S motion retains view identity before feature
pooling. There is no persistent object ID, task-start progress state, oracle
input, recurrent ODE update or replacement for remaining physical reasoning.

New parameters are in the existing logical owner ledger and optimizer groups.
An initial production test detected missing registration for P1 target_read;
the ledger and optimizer mapping were fixed, not bypassed. Initial test
fixture failures (wrong encode_online tuple arity and untyped ABI maps) were
corrected without altering assertions or hiding previous logs. Three supported
centers (reset, middle, real tail) now execute actual optimizer updates.

Deployment metadata owns the target semantics. The new file is in the source
identity closure. Ordinary exact checkpoint reload and deployment reconstruction
run the actual neural graph; no strict=False or old-checkpoint migration.
Legacy reader-local configurations retain parameters/order and same-noise
outputs at seeds 0 and 71 against the pristine M4d tree. Modes are explicit,
not automatic compatibility adapters for new trained models.

## Exact-source validation and release

Local supplemental runtime: Python 3.13.5 / PyTorch 2.10.0+cpu. It is NOT the
project's required Python 3.12 / PyTorch 2.11.x runtime. Supported CI results
must be read separately from the actual workflow/artifact, not inferred.

The final focused M5a inventory has 28 passing cases. It covers null/extreme
scores, missing-source quarantine and backward, true Q/K/value dependencies,
object/source permutation, real optimizer ownership/updates, shared-binding
identity across all numerical calls, action-only gradients without Teacher,
CPU BF16, missing metadata rejection, source identity, exact reload and
same-noise native seven-dimensional action output. Synthetic transport fixtures
are identified in the tests; G/S/W/P, optimizer, checkpoint and solver are real.

Scoped Ruff is clean and differential Pyright reports no new errors/imports.
Inherited diagnostics and additional library-stub warnings remain visible.
The complete selected local regression finished 724 collected cases: 723
passed, one skipped, zero failures/errors across 39 source-owned files.
All production/test/config hashes were unchanged during that inventory.
An additional full ownership review covered 23 new/replaced parameter tensors:
all update under ordinary training; after one unmodified training step all
receive finite nonzero policy-only VJP. At initialization the coarse reader
has zero policy VJP through inherited zero-initialized W output heads, but its
direct coarse objective works. The exploratory audit first assumed immediate
policy VJP for every owner; that assumption was corrected without editing
model parameters. A memory-killed exploratory process is not counted as a pass.
The final ownership check ran independently after full regression to avoid
retaining multiple encoder graphs alongside another neural-test process. Interrupted runs and
missing JUnit are failures, not passes or skips. Exact final counts, archive
hash and the final remote commit are recorded by the completed artifact.

The first supported CI attempt (`35707128318`) stopped before neural tests:
three new type diagnostics appeared in the broader legacy-consumer inventory,
because its attention-weight VJP selected a field before narrowing the new
reader union. The test now explicitly requires the real legacy reader and
non-null packed weights, with the original nonzero-gradient assertions intact.
No production arithmetic, static gate or tolerance was weakened. Its failed
artifact is retained. The corrected exact-source rerun is tracked separately.

No CUDA execution, production-batch memory/latency measurement, formal training
or physical closed-loop evaluation has run. CPU BF16 is not CUDA BF16.
The direct current-target path does not mathematically guarantee the trained
network will use it well, and no learned success is claimed.

## Next source work (open)

M4c2: physical objectness and persistent/cross-window association remain open.
Do not turn a K slot into a physical ID. Finite causal grouping is available.

M5b: instruction-start observation and desired/current relation must be carried
by the real dataset and online policy, with identical causal initialization and
reset. Do not use the sliding history start, episode progress scalar, future
labels or unchanged slot indices as a task reference. Audit the actual labelled
start in virtualized CALVIN records before implementation. No task-reference
source draft is included or claimed here.

M6: demonstration-action-matched W supervision versus policy candidate W,
robot-object relations, known control horizon and future recognizer targets.
M7: deeper target-local P1 spatial reads and view-aware physical consequences.
M8: execution feedback for the actually executed prefix, once per observation.
M9-11: bottom/outlet, complete train/deploy/restore agreement and final audit.
Interface migration is not completion of those structural reviews.

Do not modify master/historical branches, server jobs or checkpoints. No formal
training. If publication fails, preserve actual nonempty source, diff, hashes
and test records and continue source work rather than repeating opaque payloads.
A transport commit is not a verified source release.
